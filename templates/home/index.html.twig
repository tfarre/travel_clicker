{% extends 'base.html.twig' %}

{% block title %}Travel Clicker - Home{% endblock %}

{% block body %}
<main class="min-h-screen bg-gradient-to-br from-blue-50 to-indigo-100">
    <div class="container mx-auto px-4 py-8">
        <header class="text-center mb-12">
            <h1 class="text-4xl font-bold text-gray-800 mb-2">🌍 Travel Clicker</h1>
            <p class="text-gray-600">A marketplace simulation idle game</p>
        </header>

        {# Svelte component integration via Symfony UX #}
        <div {{ svelte_component('Hello', { name: 'Traveler' }) }}></div>

        <section class="mt-12 text-center">
            <div class="inline-flex items-center gap-2 px-4 py-2 bg-green-100 text-green-800 rounded-full text-sm">
                <span class="w-2 h-2 bg-green-500 rounded-full animate-pulse"></span>
                Setup Complete - Ready to Build!
            </div>
        </section>
    </div>
</main>
{% endblock %}
