{% extends 'base.html.twig' %}

{% block title %}Travel Clicker - Home{% endblock %}

{% block body %}
<main class="min-h-screen bg-gradient-to-br from-blue-50 to-indigo-100 flex items-center justify-center">
    <div class="container mx-auto px-4 py-8 text-center">
        <header class="mb-12">
            <h1 class="text-5xl font-bold text-gray-800 mb-4">🌍 Travel Clicker</h1>
            <p class="text-xl text-gray-600 max-w-2xl mx-auto">
                Développez votre marketplace de voyages et devenez le leader du tourisme en ligne !
            </p>
        </header>

        <div class="mb-12">
            <a
                href="{{ path('app_game') }}"
                class="inline-block px-8 py-4 text-2xl font-bold text-white bg-gradient-to-r from-blue-500 to-indigo-600 rounded-xl shadow-lg hover:from-blue-600 hover:to-indigo-700 hover:shadow-xl transform hover:scale-105 transition-all duration-200"
            >
                🎮 Jouer
            </a>
        </div>

        <section class="max-w-3xl mx-auto">
            <h2 class="text-2xl font-semibold text-gray-700 mb-6">Comment jouer ?</h2>

            <div class="grid grid-cols-1 md:grid-cols-3 gap-6 text-left">
                <div class="bg-white rounded-xl shadow-md p-6">
                    <div class="text-3xl mb-3">🖱️</div>
                    <h3 class="font-semibold text-gray-800 mb-2">Cliquez</h3>
                    <p class="text-gray-600 text-sm">
                        Attirez des visiteurs sur votre marketplace en cliquant.
                    </p>
                </div>

                <div class="bg-white rounded-xl shadow-md p-6">
                    <div class="text-3xl mb-3">📢</div>
                    <h3 class="font-semibold text-gray-800 mb-2">Investissez</h3>
                    <p class="text-gray-600 text-sm">
                        Achetez du marketing pour attirer des visiteurs automatiquement.
                    </p>
                </div>

                <div class="bg-white rounded-xl shadow-md p-6">
                    <div class="text-3xl mb-3">🤝</div>
                    <h3 class="font-semibold text-gray-800 mb-2">Partenariats</h3>
                    <p class="text-gray-600 text-sm">
                        Signez des partenaires pour augmenter vos commissions.
                    </p>
                </div>
            </div>
        </section>

        <section class="mt-12">
            <div class="inline-flex items-center gap-2 px-4 py-2 bg-green-100 text-green-800 rounded-full text-sm">
                <span class="w-2 h-2 bg-green-500 rounded-full animate-pulse"></span>
                MVP en cours de développement
            </div>
        </section>
    </div>
</main>
{% endblock %}
